import React from 'react';

const Config = (props) => {
    return (
        <div>
            <ul>
                <li>{props.price}</li>
            </ul>
        </div>
    );
};

export default Config;